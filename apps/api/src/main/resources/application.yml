spring:
  application:
    name: open-ecosystem-api

server:
  port: ${API_PORT:8080}

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,metrics
