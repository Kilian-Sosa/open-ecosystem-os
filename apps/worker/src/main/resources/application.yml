spring:
  application:
    name: open-ecosystem-worker

server:
  port: ${WORKER_PORT:8081}

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,metrics
